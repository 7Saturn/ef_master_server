Probleme:

Verbindung zwischen sich meldendem Server und Master bleibt aufrecht?
Master-Query sollte in regelmäßigen Intervallen stattfinden. Ggf. Quelle im server Entry speichern, damit nur Server gespeichert werden, die man tatsächlich selbst gemeldet bekommen hat.
    Gedanke um alle auf einmal zu kriegen:
	    AddServerListFromMaster aufbohren, um auch version00 zu senden. Sollte er da was kriegen, braucht er gar nicht mehr durch die anderen gehen. Sonst einzeln abklappern.
	    HeartbeatListener.StartListener so umbauen, dass es bei Version 00 alle raus haut, egal ob voll oder leer, egal welche Version.
Offene Ideen:
Server Entries aus File lesen und in File speichern, dessen Name über die Konsole übergeben wird. Methoden dazu:
    string text = File.ReadAllText(@"c:\file.txt", Encoding.UTF8);
    boolean laber = File.Exists(@"c:\temp\test.txt");
    File.WriteAllText(@"c:\temp\MyTest.txt", textString);

